# Specific host configs
Include config.d/*

Host *
	#	SetEnv TERM=xterm-256color
	#	RemoteCommand TERM=xterm-256color $SHELL -l
	IdentitiesOnly no

Host yubi
	IdentityAgent ${XDG_RUNTIME_DIR}/piv-ssh-default.socket
